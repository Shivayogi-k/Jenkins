pipeline {
    agent any
      stages {
          stage ('Build Stage') {
              steps {
                    echo 'build stage running '
                    echo 'deployed to QA environment'
              }
          }
          stage ('Test Stage') {
              steps {
                    echo 'Test stage running '
                    echo 'deployed to QA environment'
              }
          }
      }
}
