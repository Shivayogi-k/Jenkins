pipeline {
    agent any
      stages {
          stage ('Test Stage') {
              steps {
                    echo 'Testing......'
                    echo 'Testing completed'
              }
          }
        stage ('Deploy Stage') {
              steps {
                    echo 'Running deploy stage'
                    echo 'deployed to UAT environment'
              }
          }
      }
}

