pipeline {
    agent any
      stages {
          stage ('QA Stage') {
              steps {
                    echo 'build stage running ${params.branch}'
                    echo 'deployed to dev environment'
              }
          }
      }
}
